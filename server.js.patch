--- server.js	2025-08-17 01:00:00
+++ server.js	2025-08-17 01:00:00
@@ -50,12 +50,7 @@
 app.get('/', (req, res) => {
   if (req.session.user) {
     // Kiểm tra xem người dùng có muốn sử dụng giao diện hiện đại không
-    const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-    
-    if (useModernUI) {
-      res.sendFile(join(__dirname, 'views', 'modern-home.html'));
-    } else {
-      res.sendFile(join(__dirname, 'views', 'home.html'));
-    }
+    res.sendFile(join(__dirname, 'views', 'modern-home.html'));
   } else {
     res.sendFile(join(__dirname, 'index.html'));
   }
@@ -66,13 +61,7 @@
   if (req.session.user) {
     return res.redirect('/');
   }
-  
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-login.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'login.html'));
-  }
+
+  res.sendFile(join(__dirname, 'views', 'modern-login.html'));
 });
 
@@ -111,13 +100,7 @@
   if (req.session.user) {
     return res.redirect('/');
   }
-  
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-register.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'register.html'));
-  }
+
+  res.sendFile(join(__dirname, 'views', 'modern-register.html'));
 });
 
@@ -161,12 +144,6 @@
 app.get('/logout', (req, res) => {
   // Lấy thông tin người dùng trước khi xóa session
   const userId = req.session.user?.id;
-  
-  // Xóa session
   req.session.destroy();
-  
-  // Xóa cookie giao diện
-  res.clearCookie('useModernUI');
-  
   // Xóa người dùng khỏi danh sách online
   if (userId) {
@@ -181,13 +158,7 @@
     return res.redirect('/login');
   }
   
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-room-battle.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'room-battle.html'));
-  }
+  res.sendFile(join(__dirname, 'views', 'modern-room-battle.html'));
 });
 
 app.get('/solo-battle', (req, res) => {
@@ -195,13 +166,7 @@
     return res.redirect('/login');
   }
   
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-solo-battle.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'solo-battle.html'));
-  }
+  res.sendFile(join(__dirname, 'views', 'modern-solo-battle.html'));
 });
 
 app.get('/history', (req, res) => {
@@ -209,13 +174,7 @@
     return res.redirect('/login');
   }
   
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-history.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'history.html'));
-  }
+  res.sendFile(join(__dirname, 'views', 'modern-history.html'));
 });
 
 app.get('/ranking', (req, res) => {
@@ -223,13 +182,7 @@
     return res.redirect('/login');
   }
   
-  const useModernUI = req.query.modern === 'true' || req.cookies.useModernUI === 'true';
-  
-  if (useModernUI) {
-    res.sendFile(join(__dirname, 'views', 'modern-ranking.html'));
-  } else {
-    res.sendFile(join(__dirname, 'views', 'ranking.html'));
-  }
+  res.sendFile(join(__dirname, 'views', 'modern-ranking.html'));
 });
 
 // API lấy thông tin người dùng hiện tại
@@ -248,18 +201,6 @@
   }
 });
 
-// API cài đặt giao diện
-app.post('/api/settings/ui', (req, res) => {
-  if (!req.session.user) {
-    return res.status(401).json({ error: 'Unauthorized' });
-  }
-  
-  const { useModernUI } = req.body;
-  res.cookie('useModernUI', useModernUI, { maxAge: 30 * 24 * 60 * 60 * 1000 }); // 30 ngày
-  
-  res.json({ success: true });
-});
-
 // API lịch sử trận đấu của người dùng
 app.get('/api/user/history', async (req, res) => {
   if (!req.session.user) {